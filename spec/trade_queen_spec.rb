require 'spec_helper'

describe TradeQueen do
  it 'has a version number' do
    expect(TradeQueen::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
